/**
*
*/

#include "../../include/Game.hpp"

void Game::move(unsigned int move)
{
    if(move == MOVE_UP)
    {

    }
    else if(move == MOVE_LEFT)
    {

    }
    else if(move == MOVE_RIGHT)
    {

    }
    else if(move == MOVE_DOWN)
    {

    }
    else
    {
        std::cout << "error move lost" << std::endl;
    }
}